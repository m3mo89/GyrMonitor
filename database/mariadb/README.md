# MariaDB Foundation

MariaDB is the central system of record for the backend.

Future work will add schema and migrations for users, cattle, activity events, alerts, observations, and sync logs. Phase 1 only reserves the structure.

## Verification

Review the folder structure and keep production credentials out of committed files.
