# SQLite Foundation

SQLite is reserved for mobile and desktop local storage used by offline-capable clients.

Future work will add local schema for cached alerts, pending events, pending observations, and sync queue items. Phase 1 only reserves the structure.
