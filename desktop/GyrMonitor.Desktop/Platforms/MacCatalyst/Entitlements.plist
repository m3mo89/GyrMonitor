<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <!-- See https://aka.ms/maui-publish-app-store#add-entitlements for more information about adding entitlements.-->
    <dict>
        <!-- App Sandbox must be enabled to distribute a MacCatalyst app through the Mac App Store. -->
        <key>com.apple.security.app-sandbox</key>
        <true/>
        <!-- When App Sandbox is enabled, this value is required to open outgoing network connections. -->
        <key>com.apple.security.network.client</key>
        <true/>
        <!-- Mac Catalyst uses iOS-style Keychain APIs, which always require an access group
             entitlement (independent of App Sandbox) - without it, SecureStorage.Default.SetAsync
             throws "Error adding record: MissingEntitlement". $(AppIdentifierPrefix) resolves to the
             Team ID and only expands correctly when signed with a real identity/provisioning
             profile (see CodesignKey/CodesignProvision in GyrMonitor.Desktop.csproj), not ad hoc. -->
        <key>keychain-access-groups</key>
        <array>
            <string>$(AppIdentifierPrefix)com.companyname.gyrmonitor.desktop</string>
        </array>
    </dict>
</plist>

