# Layouts Boundary

`AppShell` renders the authenticated shell: sidebar navigation filtered by the current user's role, the active session indicator, and the sign-out action. It wraps every routed page rendered by `app/router`.
