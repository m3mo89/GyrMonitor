# Shared Components

`UiState` renders neutral/warning/danger empty, error, and info states with an optional action. `LoadingState` renders the standard loading indicator. Used across every feature page for loading, empty, and error states.
