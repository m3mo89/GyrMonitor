## 1. Shared Kernel

- [x] 1.1 Create `backend/src/shared/http/api-response.ts` with generic `ApiSuccess<T>` and `ApiError` types
- [x] 1.2 Build a mapping table from the 6 controllers' current `toHttpError()` implementations (domain error class → HTTP status + error code), documented as a comment or table in the new filter file
- [x] 1.3 Create `backend/src/shared/http/domain-error.filter.ts` implementing NestJS `ExceptionFilter`, using the mapping table from 1.2 (implemented via a `DomainError` base class in `shared/domain/domain-error.ts` instead of importing every concrete error class into the filter — avoids the filter depending on every feature module; see note below)
- [x] 1.4 Register the filter globally in `backend/src/main.ts` via `app.useGlobalFilters(...)`
- [x] 1.5 Create `backend/src/shared/validation/assertions.ts` and move `assertUuid`/`assertIsoDateTime` there (removed from `activity-events/domain/activity-event.ts`); also consolidated two other independent duplicate copies found during implementation (`inspections/domain/observation.ts`, `offline-sync/domain/sync-log.ts`) and `cattle-monitoring/application/uuid.ts` (deleted, was `isUuid`-only)
- [x] 1.6 Update all import sites of `assertUuid`/`assertIsoDateTime`/`isUuid`/`assertNonEmptyString` (`alerts`, `dashboard`, `offline-sync`, `inspections`, `cattle-monitoring`, `activity-events` itself) to import from `shared/validation/assertions`
- [x] 1.7 Run `npx tsc --noEmit` to confirm no broken imports (also required adding `@types/express` as a devDependency, needed by the new filter's `Response` type)

## 2. Migrate Controllers to the Shared Filter (one module at a time)

- [x] 2.1 Migrate `cattle.controller.ts`: remove local `ApiSuccess`/`ApiError`/`apiError`/`toHttpError`, remove its `try/catch`, import types from `shared/http/api-response`; run `npm run test:unit` and manually smoke-test `GET /cattle`, `GET /cattle/:id`, `GET /cattle/:id/events`
- [x] 2.2 Migrate `alerts.controller.ts` the same way; smoke-test `GET /alerts`, `GET /alerts/:id`, `PATCH /alerts/:id/status`
- [x] 2.3 Migrate `dashboard.controller.ts` the same way; smoke-test `GET /dashboard`
- [x] 2.4 Migrate `observations.controller.ts` the same way; smoke-test the `alerts/:alertId/observations` routes
- [x] 2.5 Migrate `activity-events.controller.ts` the same way; smoke-test `POST /events`, `GET /events`
- [x] 2.6 Migrate `offline-sync.controller.ts` the same way; smoke-test `POST /sync/events`, `POST /sync/observations`, `GET /sync/status`
- [x] 2.7 Confirm no controller file still defines a local `toHttpError`/`apiError` (`grep -rn "function toHttpError" backend/src` returns nothing) — verified; also had to update `scripts/check-foundation.mjs`'s file manifest (it still referenced the deleted `cattle-monitoring/application/uuid.ts` and didn't list the new `shared/**` files) and refresh the stale `shared/README.md` placeholder text. Full HTTP smoke test against a live MariaDB confirmed identical status codes/envelopes for success, 400 validation, and 404 not-found paths across `cattle`, `alerts`, and `dashboard`.

## 3. Break the cattle-monitoring ⟷ activity-events Cycle

- [x] 3.1 In `ActivityEventsModule`, export the `ActivityEventRepository` provider token so other modules can inject it (added `ACTIVITY_EVENT_REPOSITORY` symbol token in `activity-event.types.ts` since the repository is a structural type, not a class, so it needs an explicit DI token)
- [x] 3.2 In `CattleMonitoringModule`, add `imports: [ActivityEventsModule]` and inject `ActivityEventRepository` into `GetCattleHistoryUseCase` via Nest DI (`useFactory` + `inject: [ACTIVITY_EVENT_REPOSITORY]`) instead of importing `sharedActivityEventRepository` from `activity-events/infrastructure/activity-event-singletons` directly
- [x] 3.3 Remove the now-unused direct import of `activity-events/infrastructure/activity-event-singletons` from `cattle-monitoring/infrastructure/cattle-singletons.ts`
- [x] 3.4 Confirm `activity-events` still obtains `sharedCattleRepository` from `cattle-monitoring/infrastructure/cattle-repository-singleton` (the one allowed direction) — no change needed here per design.md
- [x] 3.5 Run `npm run build` and `npm run test:unit`; manually smoke-test `GET /cattle/:id/events` — 200 with correct paginated event history against live MariaDB

## 4. Standardize Remaining Cross-Module Wiring on Nest DI

- [x] 4.1 Convert `alerts`' direct import of `cattle-monitoring/infrastructure/cattle-repository-singleton` to `CattleMonitoringModule` `imports`/`exports` (new `CATTLE_REPOSITORY` symbol token) + constructor injection into a Nest-managed `RepositoryAlertCattleLookup` provider, consumed by `ListAlertsUseCase`/`GetAlertDetailUseCase` factories
- [x] 4.2 Convert `alerts`' direct import of `inactivity-analysis/infrastructure/inactivity-analysis-singletons` — **partially deferred, documented**: while implementing this, discovered a third, previously-unlisted cross-module edge (`activity-events` raw-imports `generateAlertFromActivityEventUseCase` from `alerts/infrastructure/alert-singletons`). Fully converting `GenerateAlertFromActivityEventUseCase`'s risk-calculator dependency to Nest DI would require `ActivityEventsModule` to import `AlertsModule`, which combined with `AlertsModule → CattleMonitoringModule → ActivityEventsModule` (from 4.1) forms a genuine 3-module Nest cycle. Left `generateAlertFromActivityEventUseCase` as a directly-constructed singleton (still importing `mvpRiskCalculator` raw) with the reasoning documented inline in `alert-singletons.ts`; `AlertsModule` reuses that same singleton via `useValue` rather than reconstructing it. Resolving this fully (via `forwardRef()` or restructuring) is out of scope — see design.md Open Questions.
- [x] 4.3 Convert `offline-sync`'s direct imports of `inspections/infrastructure/observation-singletons` and `activity-events/infrastructure/activity-event-singletons` to Nest DI module imports/exports (new `OBSERVATION_REPOSITORY` token, `AddAlertObservationUseCase`/`RegisterActivityEventUseCase` added to their modules' `exports`)
- [x] 4.4 Grepped remaining `from '../../<module>/` cross-module imports: only 2 raw value imports remain, both documented exceptions (`alerts/infrastructure/alert-singletons.ts` → `mvpRiskCalculator`; `activity-events/infrastructure/activity-event-singletons.ts` → `sharedCattleRepository` (allowed direction, task 3.4) and `generateAlertFromActivityEventUseCase` (see 4.2)). All other remaining cross-module imports are `import type` (domain types), which are acceptable per this task's own criteria. `inspections` and `offline-sync` now have zero cross-module infrastructure imports.

## 5. Verification

- [x] 5.1 Run `npm run build`, `npm run lint`, `npm run test` (unit + e2e) from `backend/` — all green (build, lint, 45 unit tests, 31 e2e tests). Along the way found and fixed a real regression: `test/http-test-app.ts` (the shared e2e test harness) builds its own `INestApplication` via `Test.createTestingModule(...)` independent of `main.ts`'s `bootstrap()`, so it never registered the new global `DomainErrorFilter` — e2e tests were getting 500s instead of the expected 400/404/409. Fixed by adding `app.useGlobalFilters(new DomainErrorFilter())` there too. Also ran `npm run db:check` (not strictly required by this task, but exercises real MariaDB-backed repositories) and found `scripts/check-database.mjs` directly required `getAlertDetailUseCase`/`listAlertsUseCase` from `alert-singletons.js`, which no longer exports them since their construction moved into `AlertsModule`'s Nest factories (task 4.1) — fixed by having the script construct them directly the same way the module factory does. After that fix, all of `db:check` passes except one hardcoded assertion that a specific alert is `PENDING`; direct SQL query confirmed that alert is `ATTENDED` in the shared local dev database (pre-existing state unrelated to this refactor — no code in this change touches alert status or query logic, only how objects are wired together).
- [x] 5.2 Start the backend locally and use `/api/docs` (or targeted `curl`) to confirm every route's status codes and response shapes are unchanged from before this change — swept all 6 protected+public route groups (200 each with a valid token) plus `/api/docs-json` (still 14 documented paths); combined with the per-controller smoke tests done during tasks 2-4 (400/404/409 cases), status codes and envelopes are unchanged from before the refactor
- [x] 5.3 Re-ran the cross-module import grep from 4.4: same 3 documented exceptions, no new ones; module `imports` graph (`AlertsModule → {InactivityAnalysisModule, CattleMonitoringModule → ActivityEventsModule}`, `OfflineSyncModule → {ActivityEventsModule, InspectionsModule}`) confirmed acyclic

## 6. Spec Sync

- [x] 6.1 Run `/opsx:sync` (or equivalent) to promote `specs/backend-architecture/spec.md` into `openspec/specs/` once implementation lands
